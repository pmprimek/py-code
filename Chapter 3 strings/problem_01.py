# Write a python program to display a user entered name followed by Good
# Afternoon using input () function.

name = input ("enter your name : ")
print(f"good afternoon, {name}") # pF-String (Formatted String Literal) is used in Python to directly insert variables and expressions into a string.